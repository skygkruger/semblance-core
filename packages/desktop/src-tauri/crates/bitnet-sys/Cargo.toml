[package]
name = "bitnet-sys"
version = "0.1.0"
edition = "2021"
description = "FFI bindings to BitNet.cpp (llama.cpp fork with 1-bit kernel support)"
authors = ["Veridian Synthetics"]
license = "MIT"

[build-dependencies]
# cmake invoked directly via std::process::Command for ClangCL toolset control.
# cc crate compiles the bitnet-stubs.cpp for MAD-only builds.
cc = "1"

[dependencies]
# Pure FFI crate — no runtime dependencies.
